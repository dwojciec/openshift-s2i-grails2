#!/bin/bash -e

pushd ${HOME} > /dev/null

# ${HOME}/.grails is the Grails repo dir
# ${HOME}/source/target is the class files from the previous build
mkdir -p ./.grails ./.m2 2> /dev/null
tar -cf - ./.grails ./.m2

popd >/dev/null
